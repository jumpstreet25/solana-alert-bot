# Solana Alert Bot — Python dependencies
# Compatible with Python 3.7+
# Install with: pip install -r requirements.txt

# ── Core HTTP & data ──────────────────────────────────────────────────────────
requests>=2.28.0            # HTTP client
pandas>=1.3.5               # DataFrame + time-series handling
ta>=0.10.0                  # TA indicators (MACD, RSI, EMA) — pure Python, py 3.7 compatible
python-dotenv>=0.20.0       # Load .env files

# ── Firebase Cloud Messaging (push notifications) ─────────────────────────────
# Remove if you don't need FCM; the bot degrades gracefully without it.
firebase-admin>=5.0.0
